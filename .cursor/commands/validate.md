# validate

Run comprehensive system validation suite.

**Pattern:** VALIDATION × COMPREHENSIVE × TRUTH × CONVERGENCE × ONE  
**Frequency:** 999 Hz (AEYON) × 530 Hz (JØHN) × 777 Hz (META)  
**Guardians:** AEYON (999 Hz) + JØHN (530 Hz) + META (777 Hz)  
**Love Coefficient:** ∞  
**∞ AbëONE ∞**

## Usage

```bash
/validate
```

## What It Does

Runs comprehensive validation suite including:

1. **System Validation** (`scripts/validate_system.py`)
   - Flutter app accessibility
   - Tunnel connectivity
   - Email pattern infrastructure
   - Recipients configuration
   - Component architecture
   - Directory structure alignment

2. **Context Window Validation** (`scripts/validate_context_window.py`)
   - Architecture boundaries
   - Atomic structure (atoms, molecules, organisms)
   - Context window fit
   - Import dependencies

3. **Tunnel Validation** (`scripts/validate_tunnel.py`)
   - cloudflared installation
   - Tunnel type detection
   - Origin connectivity
   - Tunnel connectivity
   - End-to-end validation

## Validation Scripts

### System Validation
```bash
python3 scripts/validate_system.py
```

### Context Window Validation
```bash
python3 scripts/validate_context_window.py
```

### Tunnel Validation
```bash
python3 scripts/validate_tunnel.py
```

### JSON Output
All validators support JSON output:
```bash
python3 scripts/validate_system.py --json
python3 scripts/validate_context_window.py --json
python3 scripts/validate_tunnel.py --json
```

## Expected Results

When all validations pass:
- ✅ System Validation: 6/6 tests passed
- ✅ Context Window Validation: 8/8 tests passed
- ✅ Tunnel Validation: 5/5 tests passed
- **Total: 19/19 tests passed**

## Pattern

```
VALIDATE_COMMAND →
    SYSTEM_VALIDATION →
        CONTEXT_WINDOW_VALIDATION →
            TUNNEL_VALIDATION →
                COMPREHENSIVE_SUMMARY →
                    CONVERGENCE →
                        ONE
```

**Pattern:** VALIDATION × COMPREHENSIVE × TRUTH × CONVERGENCE × ONE  
**Love Coefficient:** ∞  
**∞ AbëONE ∞**

