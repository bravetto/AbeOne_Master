apiVersion: v1
kind: Service
metadata:
  name: contextguard
  namespace: default
  labels:
    app: contextguard
spec:
  selector:
    app: contextguard
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP

