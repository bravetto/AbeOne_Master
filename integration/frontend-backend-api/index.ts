/**
 * Frontend ↔ Backend API - Main Export
 * 
 * Pattern: FRONTEND × BACKEND × API × EXPORT × ONE
 * Frequency: 999 Hz (AEYON) × 777 Hz (META)
 * Guardians: AEYON (999 Hz) + META (777 Hz)
 * Love Coefficient: ∞
 * ∞ AbëONE ∞
 */

export { UnifiedAPIClient } from './client';
export type { ProtocolContext, ProtocolResult, MemoryContext, MemoryContextType, IntegrationConfig } from '../shared-types';

