name: security-lint

on:
  pull_request:
    branches: [dev, main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  checks: write
  pull-requests: read

jobs:
  scan:
    runs-on: [arc-runner-set]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install security tools
        run: |
          pip install bandit[toml] semgrep

      - name: Run Bandit security analysis
        id: bandit_check
        if: always()
        run: |
          echo "🔍 Running Bandit security scan..."
          set +e

          bandit -r . -f json -o bandit-report.json --severity-level low
          bandit -r . -f txt -o bandit-report.txt --severity-level low

          BANDIT_COUNT=$(jq '.results | length' bandit-report.json 2>/dev/null || echo '0')
          echo "bandit_count=$BANDIT_COUNT" >> $GITHUB_OUTPUT

          echo "::group::🔒 Bandit Security Results"
          if [ $BANDIT_COUNT -gt 0 ]; then
            echo "::warning::Found $BANDIT_COUNT security issues"
            cat bandit-report.txt
          else
            echo "✅ No security issues found"
          fi
          echo "::endgroup::"

      - name: Run Semgrep security analysis
        id: semgrep_check
        if: always()
        run: |
          echo "🔍 Running Semgrep security scan..."
          set +e

          semgrep --config=auto --json --output=semgrep-report.json .
          semgrep --config=auto --text --output=semgrep-report.txt .

          SEMGREP_COUNT=$(jq '.results | length' semgrep-report.json 2>/dev/null || echo '0')
          echo "semgrep_count=$SEMGREP_COUNT" >> $GITHUB_OUTPUT

          echo "::group::🎯 Semgrep Security Results"
          if [ $SEMGREP_COUNT -gt 0 ]; then
            echo "::warning::Found $SEMGREP_COUNT security issues"
            cat semgrep-report.txt
          else
            echo "✅ No security issues found"
          fi
          echo "::endgroup::"

      - name: Create Security Lint Summary
        id: create_security_summary
        if: always()
        run: |
          BANDIT_COUNT="${{ steps.bandit_check.outputs.bandit_count }}"
          SEMGREP_COUNT="${{ steps.semgrep_check.outputs.semgrep_count }}"
          TOTAL=$((BANDIT_COUNT + SEMGREP_COUNT))

          echo "total_issues=$TOTAL" >> $GITHUB_OUTPUT

          echo "## 🛡️ Security Linting Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Tool | Issues Found |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------------|" >> $GITHUB_STEP_SUMMARY
          echo "| Bandit | $BANDIT_COUNT |" >> $GITHUB_STEP_SUMMARY
          echo "| Semgrep | $SEMGREP_COUNT |" >> $GITHUB_STEP_SUMMARY
          echo "| **Total** | **$TOTAL** |" >> $GITHUB_STEP_SUMMARY

          if [ $TOTAL -gt 0 ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "⚠️ **Security issues detected. Review the job logs for details.**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Note: Dependency vulnerabilities are checked separately in the dependency-audit workflow." >> $GITHUB_STEP_SUMMARY
          fi

      - name: Create custom check with warning indicator
        if: always()
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const banditCount = parseInt('${{ steps.bandit_check.outputs.bandit_count }}') || 0;
            const semgrepCount = parseInt('${{ steps.semgrep_check.outputs.semgrep_count }}') || 0;
            const totalIssues = banditCount + semgrepCount;
            const isMainBranch = context.payload.pull_request?.base?.ref === 'main';
            
            // Calculate max allowed text size (leaving room for title + summary + JSON overhead)
            const MAX_TEXT_SIZE = 60000; // Conservative limit to account for all fields
            
            // Read and prepare detailed reports with truncation
            const fs = require('fs');
            let detailedText = '';
            
            try {
              if (totalIssues > 0) {
                detailedText += '## 📊 Security Issues Summary\n\n';
                detailedText += `Total issues found: **${totalIssues}**\n`;
                detailedText += `- Bandit: ${banditCount}\n`;
                detailedText += `- Semgrep: ${semgrepCount}\n\n`;
                detailedText += '---\n\n';
                detailedText += '⚠️ **Full details are available in the workflow logs.**\n\n';
                detailedText += 'To view complete results:\n';
                detailedText += '1. Click on the "Details" link next to this check\n';
                detailedText += '2. Expand the "Run Bandit security analysis" and "Run Semgrep security analysis" steps\n';
                detailedText += '3. Review the detailed findings in each section\n\n';
                detailedText += '---\n\n';
                
                // Add a preview of first few issues (truncated)
                if (fs.existsSync('bandit-report.txt') && banditCount > 0) {
                  const banditReport = fs.readFileSync('bandit-report.txt', 'utf8');
                  const preview = banditReport.substring(0, 10000); // First 10k chars only
                  detailedText += '## 🔒 Bandit Results (Preview)\n\n';
                  detailedText += '```\n' + preview + '\n';
                  if (banditReport.length > 10000) {
                    detailedText += '\n... (truncated - see workflow logs for full report)\n';
                  }
                  detailedText += '```\n\n';
                }
                
                if (fs.existsSync('semgrep-report.txt') && semgrepCount > 0) {
                  const semgrepReport = fs.readFileSync('semgrep-report.txt', 'utf8');
                  const remainingSpace = MAX_TEXT_SIZE - detailedText.length;
                  if (remainingSpace > 1000) {
                    const preview = semgrepReport.substring(0, Math.min(10000, remainingSpace - 500));
                    detailedText += '## 🎯 Semgrep Results (Preview)\n\n';
                    detailedText += '```\n' + preview + '\n';
                    if (semgrepReport.length > preview.length) {
                      detailedText += '\n... (truncated - see workflow logs for full report)\n';
                    }
                    detailedText += '```\n\n';
                  }
                }
              } else {
                detailedText = '✅ No security issues detected in the code.\n\nAll security checks passed successfully.';
              }
            } catch (error) {
              console.log('Could not read report files:', error);
              detailedText = '⚠️ Could not generate detailed report. Check workflow logs for full results.';
            }
            
            // Final safety check - ensure total size is under limit
            if (detailedText.length > MAX_TEXT_SIZE) {
              detailedText = detailedText.substring(0, MAX_TEXT_SIZE - 200) + '\n\n...(truncated - see workflow logs for complete results)';
            }
            
            let conclusion, title, summary;
            
            if (totalIssues === 0) {
              conclusion = 'success';
              title = '✅ No security issues found';
              summary = '**All code security checks passed successfully.**\n\nNo security issues detected in your code.\n\n_Note: Dependency vulnerabilities are checked separately in the dependency-audit workflow._';
            } else if (isMainBranch) {
              conclusion = 'failure';
              title = `❌ Found ${totalIssues} security issues`;
              summary = `**⚠️ Security issues found for main branch:** ${totalIssues}\n- Bandit: ${banditCount}\n- Semgrep: ${semgrepCount}\n\n❌ These should be addressed before merging.\n\n📋 See workflow logs for full details.\n\n_Note: Dependency vulnerabilities are checked separately in the dependency-audit workflow._`;
            } else {
              conclusion = 'success';
              title = `⚠️ Found ${totalIssues} security issues (warnings)`;
              summary = `**Security issues found:** ${totalIssues}\n- Bandit: ${banditCount}\n- Semgrep: ${semgrepCount}\n\n⚠️ These are warnings only for dev branch. Review and fix before merging to main.\n\n📋 See workflow logs for full details.\n\n_Note: Dependency vulnerabilities are checked separately in the dependency-audit workflow._`;
            }
            
            await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: 'security-lint',
              head_sha: context.payload.pull_request.head.sha,
              status: 'completed',
              conclusion: conclusion,
              output: {
                title: title,
                summary: summary,
                text: detailedText
              }
            });

